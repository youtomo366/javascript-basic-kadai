let weight; // 体重 (kg)
let height; // 身長 (m)
let bmi;    // BMIを保存するための変数

weight = 68;
height = 1.7;

// BMIを計算し、変数に代入します
bmi = weight / (height * height);

// BMIの結果を表示します
console.log(bmi);